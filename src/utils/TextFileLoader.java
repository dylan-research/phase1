package utils;

import java.io.IOException;


public class TextFileLoader {

	// name of file to load
	public static String[] load( String fileName ) throws IOException {
		// return null: if the fileName is invalid ( "" or null )
		// throw IOException: if the file could not be found
		// throw IOEXception: if the file could not be opened or read
		
		// return each line of text in the file as an array of Strings
		return null;
	}
	
	
}
